import styled from 'styled-components';

export const AboutWrapper = styled.section`
  color: #fff;
  margin: auto;
  max-width: 70rem;
  padding: 2rem 5rem;
`;

export const AboutTitle = styled.h1`
  font-size: 3rem;
  font-weight: 700;
  margin: 0 0 2rem 0;
`;

export const AboutSubtitle = styled.h2`
  font-size: 2.2rem;
  font-weight: 700;
  margin: 1.2rem 0;
`;

export const AboutParagraph = styled.p`
  color: #fff;
  font-size: 1.25rem;
  font-weight: 300;
  line-height: 1.5;
  letter-spacing: 0.033rem;
  padding: 0.9rem 0px;
`;

export const AboutHabilityList = styled.ul`
  list-style-type: disc;
  padding-left: 1.8rem;
`;

export const HabilityListItem = styled.li`
  font-size: 1.25rem;
  font-weight: 300;
  padding: 0.625rem 0px;
`;
